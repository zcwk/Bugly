1.每次打包都要修改

每次打包都要改tinkerId这个参数 base-1.0.1  patch-1.0.1



打增量包时一定要改baseApkDir这个参数 定位到这个文件下(会在build/bakApk/app-**-**-** （baseApkDir 参数格式就是时间）)

和

每次打增量包都要改tinkerId这个参数


上传增量包的插件版本会在版本号后面加上versionCode(所以不要看到就很奇怪)

有一点比较坑就是增量包不一定是立刻就会下发。需要等一点时间。fuck啊。害我找了好久。打了好多的版本测试。日了。